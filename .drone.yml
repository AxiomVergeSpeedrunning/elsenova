kind: pipeline
type: docker
name: Publish

platform:
  os: linux
  arch: arm64

trigger:
  branch:
    - master

steps:
  - name: Publish
    image: plugins/docker
    environment:
      YARN_REGISTRY:
        from_secret: npm_registry_url
    settings:
      repo: markrawls/elsenova
      tags: latest
